# RoadBlockagePlugin.py ユーザマニュアル

CPWC2026 応募資料「プログラムの簡単なユーザマニュアル」

## プログラムの概要

RoadBlockagePlugin は、UC-win/Road（Forum8社製 運転・道路シミュレータ）上で動作する Python プラグインです。道路の通行止め・冠水などの発生確率を予測した CSV データ（緯度・経度・確率のグリッド）を読み込み、確率がしきい値を超える地点の最寄り車線に、視覚的な目印として **'Barricade'（バリケード）** の 3D モデルを自動配置します。

Python API は UC-win/Road 上に実際の道路障害物（`F8RoadObstructionProxy`）を新規作成する手段を持たない（既存のものを読み取ることしかできない）ため、バリケードはあくまで「ここに障害物を置くとよい」という目印です。実際の道路障害物は、この目印を参考にユーザーが UC-win/Road の道路エディタ上で手動で設置します。

さらに、任意の 1 地点を選んで周辺の交通流（平均速度・車両台数）をリアルタイムに計測・グラフ表示する **Traffic Monitor（交通流モニター）** 機能を備えています。バリケードを消してから実際の道路障害物を手動で置く／外す、という操作をしながらこのモニターを見ることで、AI車両（周辺交通）が実際の道路障害物にどう反応して速度・台数が変化するかを確認できます。

## プログラムの実行方法

1. UC-win/Road でプロジェクトを開く。
2. **シミュレーションを手動で再生（Play）状態にする。** バリケードの配置（`AddNewTransient`）はシミュレーションが再生中でないと正しく初期化されないため、プラグインを読み込む前に必ず先に再生を開始しておく。
3. UC-win/Road の Python スクリプト機能（リボンまたは Script Editor）から `RoadBlockagePlugin.py` を読み込む。このとき **[Async] チェックボックスを必ず ON にする**。本プラグインはブロッキングのイベントループを持つため、[Async] が OFF のまま実行すると UC-win/Road がフリーズしてしまう。[Async] が OFF の場合、プラグインはループに入らずログとコンソール出力にメッセージを残して安全に終了する。
4. 実行に成功すると、UC-win/Road のリボンに次の 5 つのタブが追加される。
   - **CSV**
   - **Barricades**
   - **Traffic Monitor**
   - **Find Objects**
   - **Reset**
5. 終了する場合は、UC-win/Road 側の通常のスクリプト停止操作を行う。配置済みバリケード、HUDウィンドウ、リボンUIはプラグイン側で自動的に後片付けされる。

## プログラムの操作フロー

### RoadBlockDetectionApp.pyで生成されたCSVの読み込み

1. あらかじめ `RoadBlockDetectionApp.py`（本プラグインとは別の予測プログラム）を実行し、通行止め等の発生確率を地点ごとに予測したCSVファイルを生成しておく。
2. **CSV** タブの **「Browse & Load CSV...」** ボタンをクリックする。
3. ネイティブのファイル選択ダイアログが開く（`Prediction` フォルダが存在する場合は初期表示フォルダになる）。手順1で生成したCSVファイル（`Prediction/` フォルダに出力例あり）を選択する。
4. CSVは次の列を持つ必要がある。
   - `latitude` … 緯度
   - `longitude` … 経度
   - `probability` … 通行止め等の発生確率（0〜1）
   - `side_m` … 任意列（配置処理では未使用）

   これらの列を持つCSVであれば、`RoadBlockDetectionApp.py` 以外で生成したものでも読み込み可能。
5. 読み込みに成功すると、「Loaded ファイル名: N records, probability x.xxxx-y.yyyy」という要約がラベルに表示される。数値に変換できない行は自動的に読み飛ばされる。

### バリケードの配置（道路障害物は手動でバリケードの中心に配置）

1. **Barricades** タブで以下を設定する。
   - **Probability Threshold**（既定値 0.7）… この値以上の確率を持つCSVレコードのみを配置対象にする。
   - **Max Distance to Road (m)**（既定値 5000）… 最寄り道路までの距離がこの値を超えるレコードは「道路上ではない」とみなしてスキップする。
2. **「Place Barricades from CSV」** ボタンをクリックする。
   - クリックのたびに既存の配置をすべて削除してから作り直す（差分更新はしない）。
   - しきい値を満たす各CSVレコードについて、緯度経度に最も近い道路・車線を求め、その中心地点から道路に沿って **上流・下流に5mずつ** ずらした位置に、バリケードを **1レコードあたり2本** 配置する。各バリケードは道路の進行方向に対して垂直（車線をふさぐ向き）に自動で回転して配置される。
   - 結果（配置本数、スキップ件数など）が要約ラベルに表示される。
3. **この2本のバリケードの中間地点が、実際の道路障害物を置くべき場所の目印になる。** Python APIでは道路障害物そのものを作成できないため、ユーザーはこの中間地点を目安に、UC-win/Roadの道路エディタ機能を使って **手動で** 実際の道路障害物を設置する（または、既存の道路障害物をそこに動かす／作成する）。
4. 配置したバリケードの位置を確認したい場合は、**Find Objects** タブの **Barricades** グループで Barricade Index を指定して **「Jump to Barricade」** をクリックすると、そのバリケードへメインカメラが移動する。
5. 同じ **Find Objects** タブの **Road Obstructions** グループでは、**「List Obstructions」** をクリックすると道路データに既存の道路障害物（`F8RoadObstructionProxy`、読み取り専用）が一覧表示される。Obstruction Index を指定して **「Jump to Obstruction」** をクリックすると、その障害物の位置へカメラが移動し、手動設置した道路障害物が意図した場所に置けているか確認できる。
6. 配置をやり直したい／後片付けしたい場合は **Reset** タブを使う。
   - **「Reset」** … このスクリプトが配置したバリケードのみを削除する。
   - **「Clear All」** … プロジェクト内の一時オブジェクト（Transient）を無条件に全削除するクラッシュ復旧用ボタン。通常走行中の交通車両にも影響しうるため、通常は使わない。

### モニターの表示（平均速度の可視化）

1. **Traffic Monitor** タブで以下を設定する。
   - **CSV Record Index** … 監視したいCSVレコードのインデックス（0始まり、CSV読み込み順）。
   - **Measurement Radius (m)**（既定値 50）… この半径内の車両を計測対象にする。
2. **「Start Monitoring」** をクリックする。
   - 指定したCSVレコードの緯度経度から最も近い道路上の位置を求め、その **座標を固定** して監視基準点とする（バリケードの配置有無とは独立しており、バリケードを一切置いていなくても、あるいは後で **Reset** で削除しても、同じ地点の監視を継続できる）。
   - 画面右下に半透明・最前面のHUDウィンドウが表示され、UC-win/Roadのメインウィンドウに追従する。
3. HUDウィンドウおよびリボンの詳細欄には、0.5秒間隔で以下がリアルタイム表示される。
   - **Avg Speed (km/h)** … 半径内にいる車両（`TransientCar`）の平均速度と、その推移を示す折れ線グラフ。
   - **Vehicles in Zone** … 半径内の現在の車両台数と、その推移を示す折れ線グラフ。
4. 想定する使い方は次の通り。
   1. Start Monitoring で計測を開始する。
   2. **Reset** で見た目だけのバリケードを消す。
   3. UC-win/Roadの道路エディタで、同じ地点に実際の道路障害物を **手動で** 追加／削除する。
   4. HUD上の平均速度・車両台数が、AI車両の実際の反応に応じて変化する様子を観察する。
5. 監視を終える場合は **「Stop Monitoring」** をクリックする。計測を停止し、HUDウィンドウを閉じる。
